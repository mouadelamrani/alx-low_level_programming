#include <stdio.h>
/**
 * main - starting point of this program
 * Description: print number from 0 to 10, followed by a new line
 * Return: is 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
